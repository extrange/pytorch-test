[project]
name = "pytorch-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch==2.5.0",
    "torchaudio>=2.5.0",
    "torchvision>=0.20.0",
]
